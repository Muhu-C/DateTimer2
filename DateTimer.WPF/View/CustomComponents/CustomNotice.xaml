<Window x:Class="DateTimer.WPF.View.CustomComponents.CustomNotice"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
        xmlns:local="clr-namespace:DateTimer.WPF.View.CustomComponents"
        mc:Ignorable="d"
        WindowStyle="None"
        ui:TitleBar.ExtendViewIntoTitleBar="True"
        ui:WindowHelper.UseModernWindowStyle="True"
        ResizeMode="NoResize"
        Closing="Window_Closing"
        Background="Transparent"
        FontFamily="{StaticResource DefaultFont}"
        Title="" Height="150" Width="330">
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="-1" />
    </WindowChrome.WindowChrome>
    <Border Name="OutlineBorder" CornerRadius="6" Background="#00000000" 
            BorderBrush="{DynamicResource {x:Static ui:ThemeKeys.ExpanderHeaderBorderBrushKey}}">
        <Border HorizontalAlignment="Center" VerticalAlignment="Center" 
                Background="{DynamicResource {x:Static ui:ThemeKeys.AcrylicBackgroundFillColorBaseBrushKey}}" Height="148" Width="328" CornerRadius="5">
            <Grid x:Name="MainGrid">
                <Grid.RowDefinitions>
                    <RowDefinition Height="32"/>
                    <RowDefinition Height="36"/>
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Background="#22888888">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="30"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="40"/>
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" Width="20" Height="20" Margin="8,0,0,0" RenderOptions.BitmapScalingMode="HighQuality"
                           Source="/DateTimer.WPF;Component/Data/icon.png" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" FontSize="11.5" Text="木沪时间表" VerticalAlignment="Center" Margin="10"/>
                </Grid>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" d:Text="提示" Text="{Binding NoticeText1}" VerticalAlignment="Center" 
                           Margin="16,10,0,0" FontSize="16" FontWeight="Bold"/>
                    <Button Grid.Column="1" Margin="0,10,16,0" HorizontalAlignment="Right" Padding="5" Click="Button_Click">
                        <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.GotoToday}" FontSize="12"/>
                    </Button>
                </Grid>
                <ScrollViewer Name="ScrollV" Margin="25,3,10,10" Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                    <ScrollViewer.Content>
                        <TextBlock d:Text="这是提示文本" Text="{Binding NoticeText2}" VerticalAlignment="Top" HorizontalAlignment="Left" FontSize="14"/>
                    </ScrollViewer.Content>
                </ScrollViewer>
            </Grid>
        </Border>
    </Border>

</Window>
